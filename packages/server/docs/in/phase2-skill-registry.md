All right, so for this part, I want to move the things that we currently have in the skills folder into an examples slash skills folder. And then, you know, in the example, ENV, we would point to that so that it's an easy get started thing, if you will. And then the thought is that the actual skills directory, which is going to be the thing that backs a registry, would be in a different folder. And we'd have that folder get ignored. And then we would have registry endpoints that would make it so that you can connect a GitHub repo to a skill. So that's essentially per skill you can have a GitHub repo. and you can either have it be you know one repo per per skill or you can have a GitHub repo that has a bunch of skills and it can handle flattening things into a single skills folder And then as part of that registration I don think we need to do any agent grants or anything like that since that can be separate But the point of this would be it makes it so that agents can create develop skills and then register them with the skill server. Additionally, we probably would need to have some sort of permissions to edit and maintain the skill. Probably maintain is the right word, where they could edit or delete the skill. Right now we have that single admin token, but I'm thinking that essentially we would have agents, technically could even be humans. The humans would give and give their agent their token and then it would make it so that agent can maintain things So the permissions that we granting probably need to have essentially a level The first level is execute second level is maintain and then maybe the final level is grant or something like that I think there are two years Grant is the highest level It would mean that you can give other agents those permissions Or maybe we call that admin just because that more clear And so then if you have the maintain permissions, you can pull the content of the skill and push the content of the skill. I mean, I feel a little bit silly duplicating the idea of Git since it does it so well. So I don't know, maybe I'm going to use the and maybe you can let me know what you think we could do there what you think would be a good fit It seems like a Git repository would be a naturally good fit So I hate to avoid that or hate to go in a different direction than that But I also know that you know if we have a Git repository that has a bunch of things in it are we essentially saying the agent can pull all of those and push any of those And when we when I say push, I mean, I guess we don't have to necessarily worry about the agent doing it. I guess as long as they have access to the repo, they can pull or push things to the registry. Maybe it's more like an agent can have permissions within the registry to register new skills, I guess. And the registration of skills is always just a GitHub repo that you point to. Anyways, lots to think about here. Can you brainstorm with me and write a kind of concise plan? Maybe a few different ideas?